package com.myapp.ecommerce.config;

/**
 * This class has been intentionally disabled.
 * The CorsFilter has been removed as part of the CORS configuration cleanup.
 * CORS is now handled exclusively through @CrossOrigin("*") annotations on controllers.
 */
public class CorsFilter {
    // This class is intentionally empty
    // The Filter implementation has been removed to disable custom CORS configuration
}
